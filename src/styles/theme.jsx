const colors = {
  skyblue: '#E9F3FF',
  blud: '#A6C7EF',
};

const fonts = {
  kotrahope: `
    font-family: KOTRAHOPE;
    font-size: 8rem;
    font-weight: 900;
    font-style: normal;
    line-height: normal;
    letter-spacing: normal;
  `
};

const theme = {
  colors,
  fonts
};

export default theme;
